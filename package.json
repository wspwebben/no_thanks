{
  "name": "no_thanks",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "rollup --config",
    "watch": "rollup --config --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "rollup": "^2.9.1"
  }
}
